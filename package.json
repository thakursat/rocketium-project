{
  "name": "rocketium",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm --dir backend dev\" \"pnpm --dir frontend dev\"",
    "dev:backend": "pnpm --dir backend dev",
    "dev:frontend": "pnpm --dir frontend dev",
    "deploy": "pnpm --dir frontend build && pnpm --dir backend build && rm -rf backend/dist/public && mkdir -p backend/dist/public && cp -R frontend/dist/. backend/dist/public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}